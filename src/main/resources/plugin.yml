name: ${project.name}
version: ${project.version}
main: ${project.groupId}.${project.artifactId}.${project.name}
api-version: 1.13
commands:
   lucky:
      description: Force a LuckyWorld event
      usage: /lucky luckyForce
